import "../scss/notifications.scss";
const Notifications = () => {
  return (
    <div className="main-layout">
      <section className="notifications-section">
        <h1>Notifications</h1>
        <h3>Catchup with all the activities happening around...</h3>
      </section>
    </div>
  );
};

export default Notifications;
